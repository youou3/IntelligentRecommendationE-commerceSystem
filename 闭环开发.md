# 中小型智能电商 SaaS 系统闭环开发方案

## 1. 产品定位

本系统定位为面向中小型商家的智能电商 SaaS 平台，核心能力包括 AI 智能选品、个性化商品推荐、用户画像构建、智能库存管控和自动补货。系统通过 MCP 服务调度中心统一编排业务能力，通过 SKILL 模块化技能沉淀可复用能力，通过 WORKFLOW 工作流引擎实现跨模块业务自动化。

目标不是一次性堆满功能，而是先打通“数据采集 -> 用户画像 -> 推荐/选品 -> 转化反馈 -> 库存预警 -> 自动补货 -> 运营复盘”的业务闭环，再持续迭代算法、流程和运营策略。

## 2. 核心业务闭环

### 2.1 用户行为数据闭环

用户行为数据是推荐、选品和运营决策的基础。

采集范围：

- 浏览商品：商品 ID、分类、标签、停留时长、来源页面、访问时间。
- 搜索行为：搜索词、筛选条件、排序方式、点击结果。
- 加购行为：商品 ID、数量、价格、优惠信息。
- 下单行为：商品 ID、数量、成交价、支付状态、退款状态。
- 收藏/取消收藏：商品 ID、分类、用户偏好变化。
- 曝光数据：推荐位、商品 ID、是否点击、是否转化。

落地方式：

- 前端或业务端埋点将行为事件上报至 Flask 接口。
- Flask 接口完成参数校验、用户身份识别、事件标准化。
- 标准化事件写入 MongoDB，用于保存非结构化行为日志。
- 关键业务行为同步写入 MySQL，便于统计分析和运营查询。
- 可选使用 Redis 做热点用户行为缓存，提升实时推荐响应速度。

闭环结果：

- 用户每一次行为都沉淀为画像和推荐算法的输入。
- 推荐曝光、点击、加购、购买继续反哺算法效果。
- 运营可基于行为数据识别高意向用户、低转化商品和潜力品类。

### 2.2 用户画像构建闭环

用户画像用于描述用户的兴趣偏好、消费能力、购买周期和转化阶段。

画像维度：

- 基础维度：用户 ID、注册时间、来源渠道、会员等级。
- 偏好维度：高频浏览分类、点击标签、收藏品牌、价格区间。
- 购买维度：购买频次、客单价、复购周期、退款率。
- 行为阶段：新访客、活跃浏览、强购买意向、沉睡用户、复购用户。
- 推荐反馈：推荐点击率、推荐转化率、不感兴趣商品标签。

实现方案：

- 定时任务按用户聚合 MongoDB 行为日志。
- 使用标签权重模型更新用户兴趣标签。
- 将画像结果存储在 MySQL 的用户画像表中。
- 高频画像数据可缓存到 Redis，供推荐接口快速读取。

画像更新策略：

- 实时更新：浏览、点击、加购等高频行为只更新轻量标签权重。
- 定时更新：每日或每小时重算用户偏好、购买周期、用户阶段。
- 事件触发更新：下单、退款、收藏等关键行为触发画像修正。

闭环结果：

- 用户行为不断转化为可计算的用户画像。
- 画像直接影响推荐、选品和运营动作。
- 推荐和运营结果继续反馈画像准确性。

### 2.3 个性化推荐闭环

推荐算法先采用可解释、易落地的“标签匹配 + 余弦相似度”方案，后续可扩展召回、排序和重排模型。

推荐输入：

- 用户画像标签向量。
- 商品标签向量。
- 商品销量、库存、价格、毛利、上新时间。
- 用户近期行为序列。
- 推荐位上下文，例如首页、详情页、购物车页。

推荐流程：

1. MCP 调度推荐 SKILL。
2. 推荐 SKILL 获取用户画像和候选商品。
3. 基于标签权重计算用户向量和商品向量的余弦相似度。
4. 结合库存、销量、毛利、上下架状态进行业务过滤。
5. 输出推荐商品列表，并记录曝光日志。
6. 用户点击、加购、购买后回写行为数据，形成推荐反馈。

核心指标：

- 推荐点击率 CTR。
- 推荐加购率。
- 推荐转化率。
- 推荐 GMV。
- 人均推荐曝光数。
- 推荐商品库存命中率。

闭环结果：

- 推荐结果可追踪、可解释、可复盘。
- 推荐效果通过用户行为持续回流。
- 算法策略可以按商品分类、用户阶段、推荐位逐步优化。

### 2.4 AI 智能选品闭环

智能选品用于帮助商家发现值得上架、重点推广或淘汰的商品。

选品输入：

- 商品历史销量。
- 浏览、收藏、加购、购买转化数据。
- 商品毛利率。
- 库存周转率。
- 用户偏好标签热度。
- 品类增长趋势。
- 退款率和差评率。

选品评分模型：

```text
选品分 = 需求热度分 * 0.30
       + 转化效率分 * 0.25
       + 毛利空间分 * 0.20
       + 库存健康分 * 0.15
       + 用户偏好匹配分 * 0.10
```

商品分层：

- 爆品：高浏览、高转化、高毛利，建议重点推荐和补货。
- 潜力品：高浏览、高收藏、低转化，建议优化价格、详情页或优惠。
- 长尾品：低曝光但有稳定转化，建议定向推荐。
- 风险品：库存高、转化低、退款高，建议降价、清仓或下架。

闭环结果：

- 选品策略基于真实业务数据，而不是人工经验拍脑袋。
- 选品结果影响推荐权重、补货策略和运营活动。
- 商品后续表现继续回流，修正选品评分。

### 2.5 智能库存与自动补货闭环

库存模块通过 WORKFLOW 工作流实现“库存监控 -> 风险判断 -> 预警通知 -> 补货建议 -> 审批确认 -> 自动生成补货单 -> 入库跟踪”的自动化闭环。

库存预警规则：

- 安全库存阈值：当前库存低于安全库存时触发预警。
- 销售速度预警：预计可售天数低于设定值时触发预警。
- 爆品预警：高转化商品库存不足时提升预警等级。
- 滞销预警：库存周转天数过高时触发清仓建议。

补货建议逻辑：

```text
建议补货量 = 未来 N 天预测销量 + 安全库存 - 当前可用库存 - 在途库存
```

WORKFLOW 节点：

1. 定时检查库存和销售数据。
2. 判断是否触发库存预警。
3. 调用选品 SKILL 和推荐 SKILL 获取商品优先级。
4. 生成补货建议或清仓建议。
5. 推送给商家或运营人员审批。
6. 审批通过后生成采购单或补货单。
7. 入库后更新库存状态。
8. 记录流程结果，用于后续补货策略优化。

闭环结果：

- 库存风险从人工发现变成系统主动预警。
- 补货数量由销量预测、库存状态和商品价值共同决定。
- 补货结果继续影响库存健康度和选品评分。

## 3. 系统架构设计

### 3.1 总体架构

```text
用户端 / 商家端
      |
Flask API 服务
      |
MCP 服务调度中心
      |
+----------------------+----------------------+----------------------+
| 用户画像 SKILL        | 推荐算法 SKILL        | 智能选品 SKILL        |
| 行为采集 SKILL        | 库存预警 SKILL        | 自动补货 SKILL        |
+----------------------+----------------------+----------------------+
      |
WORKFLOW 工作流引擎
      |
+----------------------+----------------------+----------------------+
| MySQL 业务数据        | MongoDB 行为日志      | Redis 可选缓存        |
+----------------------+----------------------+----------------------+
```

### 3.2 模块边界

- Flask API：负责接口服务、参数校验、鉴权、路由分发。
- MCP 调度中心：负责统一调度 SKILL，屏蔽底层能力差异。
- SKILL 模块：负责封装独立业务能力，例如推荐、画像、库存预警。
- WORKFLOW 引擎：负责编排跨模块流程，例如自动补货、库存预警。
- MySQL：保存用户、商品、订单、库存、画像、补货单等结构化数据。
- MongoDB：保存用户行为日志、推荐曝光日志、算法反馈数据。
- Redis：缓存热点画像、推荐结果、库存风险商品。

## 4. 推荐目录结构

```text
IntelligentRecommendationE-commerceSystem/
├── app/
│   ├── api/
│   │   ├── behavior_api.py
│   │   ├── recommendation_api.py
│   │   ├── product_api.py
│   │   └── inventory_api.py
│   ├── core/
│   │   ├── config.py
│   │   ├── database.py
│   │   ├── response.py
│   │   └── auth.py
│   ├── mcp/
│   │   ├── dispatcher.py
│   │   └── registry.py
│   ├── skills/
│   │   ├── behavior_collect_skill.py
│   │   ├── user_profile_skill.py
│   │   ├── recommendation_skill.py
│   │   ├── product_selection_skill.py
│   │   ├── inventory_warning_skill.py
│   │   └── replenishment_skill.py
│   ├── workflows/
│   │   ├── inventory_warning_workflow.py
│   │   └── auto_replenishment_workflow.py
│   ├── models/
│   │   ├── user.py
│   │   ├── product.py
│   │   ├── order.py
│   │   ├── inventory.py
│   │   └── user_profile.py
│   └── services/
│       ├── behavior_service.py
│       ├── recommendation_service.py
│       ├── inventory_service.py
│       └── product_selection_service.py
├── docs/
│   ├── api.md
│   ├── database.md
│   ├── mcp_skill_design.md
│   └── workflow_design.md
├── tests/
├── requirements.txt
├── run.py
└── README.md
```

## 5. 数据库核心表设计

### 5.1 MySQL 表

用户表 `users`：

- `id`：用户 ID。
- `nickname`：用户昵称。
- `channel`：来源渠道。
- `member_level`：会员等级。
- `created_at`：注册时间。

商品表 `products`：

- `id`：商品 ID。
- `name`：商品名称。
- `category_id`：分类 ID。
- `tags`：商品标签。
- `price`：售价。
- `cost_price`：成本价。
- `gross_margin`：毛利率。
- `status`：上架状态。

用户画像表 `user_profiles`：

- `user_id`：用户 ID。
- `tag_vector`：用户标签向量 JSON。
- `price_preference`：价格偏好。
- `category_preference`：分类偏好。
- `user_stage`：用户阶段。
- `updated_at`：更新时间。

库存表 `inventory`：

- `product_id`：商品 ID。
- `available_stock`：可用库存。
- `locked_stock`：锁定库存。
- `in_transit_stock`：在途库存。
- `safe_stock`：安全库存。
- `updated_at`：更新时间。

补货单表 `replenishment_orders`：

- `id`：补货单 ID。
- `product_id`：商品 ID。
- `suggest_quantity`：建议补货量。
- `approved_quantity`：审批补货量。
- `status`：流程状态。
- `created_at`：创建时间。

### 5.2 MongoDB 集合

行为日志集合 `behavior_events`：

```json
{
  "user_id": "U10001",
  "event_type": "product_click",
  "product_id": "P10001",
  "category_id": "C10001",
  "tags": ["running", "summer"],
  "source": "home_recommend",
  "duration": 15,
  "created_at": "2026-06-08T10:00:00"
}
```

推荐曝光集合 `recommendation_logs`：

```json
{
  "request_id": "REQ10001",
  "user_id": "U10001",
  "scene": "home",
  "recommended_products": ["P10001", "P10002"],
  "clicked_products": ["P10001"],
  "converted_products": [],
  "created_at": "2026-06-08T10:01:00"
}
```

## 6. MCP 与 SKILL 设计

### 6.1 MCP 调度中心职责

- 统一注册 SKILL。
- 根据业务场景选择并调用 SKILL。
- 管理 SKILL 输入输出协议。
- 记录调用日志、耗时和异常。
- 为 WORKFLOW 提供统一能力入口。

### 6.2 SKILL 标准协议

每个 SKILL 建议遵循统一接口：

```python
class BaseSkill:
    name = ""
    version = "1.0.0"

    def validate(self, payload: dict) -> bool:
        raise NotImplementedError

    def execute(self, payload: dict) -> dict:
        raise NotImplementedError
```

标准返回：

```json
{
  "success": true,
  "code": "OK",
  "message": "success",
  "data": {}
}
```

首批 SKILL：

- `behavior_collect_skill`：用户行为采集与标准化。
- `user_profile_skill`：用户画像生成与更新。
- `recommendation_skill`：个性化商品推荐。
- `product_selection_skill`：智能选品评分。
- `inventory_warning_skill`：库存风险识别。
- `replenishment_skill`：自动补货建议生成。

## 7. WORKFLOW 工作流设计

### 7.1 库存预警工作流

触发方式：

- 定时触发：每小时检查一次库存。
- 事件触发：订单支付成功后检查相关商品库存。

流程：

1. 读取库存数据。
2. 读取近 7 天销量数据。
3. 调用库存预警 SKILL。
4. 判断预警等级。
5. 高风险商品写入预警记录。
6. 推送预警给商家后台。
7. 触发自动补货工作流。

### 7.2 自动补货工作流

触发方式：

- 库存预警触发。
- 商家手动触发。
- 爆品识别后自动触发。

流程：

1. 获取预警商品。
2. 调用智能选品 SKILL 获取商品价值分。
3. 调用补货 SKILL 计算建议补货量。
4. 生成补货单。
5. 商家审批。
6. 审批通过后生成采购单。
7. 入库后更新库存。
8. 记录实际销售表现，修正补货模型。

## 8. 接口清单

### 8.1 行为采集接口

`POST /api/behavior/collect`

请求参数：

- `user_id`
- `event_type`
- `product_id`
- `category_id`
- `tags`
- `source`
- `duration`

返回结果：

- 行为事件接收状态。

### 8.2 推荐接口

`GET /api/recommendations`

请求参数：

- `user_id`
- `scene`
- `limit`

返回结果：

- 推荐商品列表。
- 推荐请求 ID。

### 8.3 选品评分接口

`POST /api/products/selection-score`

请求参数：

- `product_ids`
- `date_range`

返回结果：

- 商品选品评分。
- 商品分层。
- 运营建议。

### 8.4 库存预警接口

`GET /api/inventory/warnings`

请求参数：

- `warning_level`
- `category_id`
- `status`

返回结果：

- 库存预警列表。

### 8.5 补货建议接口

`POST /api/inventory/replenishment/suggest`

请求参数：

- `product_id`
- `forecast_days`

返回结果：

- 建议补货量。
- 补货原因。
- 风险说明。

## 9. 开发里程碑

### 第一阶段：基础能力闭环

目标：打通最小可用业务链路。

- 搭建 Flask 项目基础结构。
- 完成 MySQL、MongoDB 连接。
- 实现用户、商品、订单、库存基础模型。
- 实现用户行为采集接口。
- 实现行为日志写入 MongoDB。
- 实现基础用户画像生成。
- 实现标签匹配推荐接口。

验收标准：

- 能采集用户行为。
- 能生成用户画像。
- 能返回个性化推荐商品。
- 能记录推荐曝光和点击反馈。

### 第二阶段：推荐与选品增强

目标：让推荐和选品具备业务可用性。

- 实现商品标签向量化。
- 实现用户标签权重更新。
- 实现余弦相似度推荐算法。
- 实现推荐业务过滤策略。
- 实现智能选品评分模型。
- 实现商品分层和运营建议。

验收标准：

- 推荐结果可解释。
- 商品可按爆品、潜力品、长尾品、风险品分层。
- 推荐点击、加购、购买数据可回流分析。

### 第三阶段：库存与补货工作流

目标：打通库存自动化流程。

- 实现库存预警 SKILL。
- 实现补货建议 SKILL。
- 实现库存预警 WORKFLOW。
- 实现自动补货 WORKFLOW。
- 实现补货单生成与状态流转。
- 实现商家后台库存预警列表。

验收标准：

- 系统能自动识别库存风险。
- 系统能生成补货建议。
- 补货流程有完整状态记录。
- 入库结果能反哺库存和选品模型。

### 第四阶段：MCP/SKILL 标准化

目标：提升系统扩展能力。

- 实现 MCP 注册中心。
- 统一 SKILL 输入输出协议。
- 增加 SKILL 调用日志。
- 增加 SKILL 异常降级机制。
- 将推荐、选品、库存、补货能力全部改造成 SKILL。
- WORKFLOW 只通过 MCP 调用能力。

验收标准：

- 新增 SKILL 无需改动主流程。
- WORKFLOW 能编排多个 SKILL。
- 每次能力调用可追踪、可监控、可复盘。

### 第五阶段：运营复盘与数据看板

目标：形成 SaaS 产品可持续运营能力。

- 推荐效果看板。
- 用户画像分布看板。
- 商品选品看板。
- 库存风险看板。
- 补货效果复盘。
- 商家维度数据隔离。

验收标准：

- 商家能看到推荐、库存、选品效果。
- 运营能识别问题商品和高价值用户。
- 系统能基于数据持续优化策略。

## 10. 测试与验收策略

单元测试：

- 标签权重计算。
- 余弦相似度计算。
- 推荐过滤逻辑。
- 选品评分逻辑。
- 补货量计算逻辑。

接口测试：

- 行为采集接口。
- 推荐接口。
- 选品评分接口。
- 库存预警接口。
- 补货建议接口。

集成测试：

- 行为采集到用户画像更新。
- 用户画像到推荐结果生成。
- 推荐曝光到反馈日志写入。
- 订单支付到库存扣减。
- 库存预警到补货单生成。

验收指标：

- 推荐接口平均响应时间小于 300ms。
- 行为采集接口成功率大于 99%。
- 库存预警任务能稳定定时执行。
- 补货单状态流转完整可追踪。
- 核心 WORKFLOW 失败后可重试。

## 11. 风险与应对

数据不足风险：

- 初期采用规则推荐和热门商品兜底。
- 新用户使用渠道、分类、热门商品做冷启动。
- 新商品使用商品标签、品类热度和毛利作为初始权重。

算法效果不稳定：

- 保留推荐解释字段，方便运营排查。
- 先上线可解释算法，再逐步引入复杂模型。
- 按场景配置不同推荐策略。

库存预测不准确：

- 初期使用近 7 天、近 30 天销量均值。
- 对爆品、季节品、长尾品使用不同预测窗口。
- 保留人工审批，避免自动补货失控。

模块耦合风险：

- 所有能力通过 MCP 调度。
- SKILL 保持单一职责。
- WORKFLOW 只负责流程编排，不直接写复杂业务逻辑。

## 12. 最小可行版本 MVP

MVP 范围：

- 用户行为采集。
- 用户画像生成。
- 个性化推荐。
- 推荐反馈日志。
- 库存预警。
- 补货建议。

MVP 暂不包含：

- 复杂机器学习模型。
- 多租户计费。
- 高级 BI 看板。
- 自动采购系统对接。
- 复杂营销自动化。

MVP 成功标准：

- 用户行为能持续沉淀。
- 推荐能基于用户画像动态变化。
- 商品推荐效果可通过点击和购买反馈衡量。
- 库存风险能自动发现。
- 补货建议能被商家用于实际决策。

## 13. 后续演进方向

- 引入协同过滤、召回排序模型和深度学习推荐模型。
- 增加 A/B 测试能力，对比不同推荐策略。
- 接入外部电商平台数据，增强选品能力。
- 增加商家多租户隔离、权限和计费体系。
- 将 WORKFLOW 扩展到营销活动、用户召回和售后处理。
- 将 SKILL 沉淀为插件市场，支持按行业快速复用。

